{
  "name": "intellmeet",
  "version": "1.0.0",
  "description": "Root package.json to run client and server concurrently",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server/server\"",
    "install-all": "npm install && npm install --prefix client && npm install --prefix server/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
